FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Compile i18n messages (REQUIRED before running)
RUN npm run lingui:compile

EXPOSE 3000

CMD ["npm", "run", "dev"]
